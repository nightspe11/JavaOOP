package hw5;

import java.util.ArrayList;
import java.util.List;

/*public class DeathTrain implements Train {
	private final int countCarrigeInitial;
	private int countCarrigeSeeking;
	private List<Carrige> train = new ArrayList<>();
	
	
	public void getDeathTrain(){
        while (train.size() < (int)(101*Math.random())){
            train.add(new Carrige());
        }
}

	@Override
    public void turnLeft() {
        if (wagons.listIterator().hasNext()) {
            numberOfWagon = 0;
        }
        else wagons.get(numberOfWagon++);
    }

    @Override
    public void turnRight() {
        if (numberOfWagon == 1) numberOfWagon = wagons.size()-1;
        else wagons.get(numberOfWagon--);
    }

    @Override
    public void lightOn() {
        wagons.get(numberOfWagon).setLight(true);
    }

    @Override
    public void lightOff() {
        wagons.get(numberOfWagon).setLight(false);
    }

    @Override
    public boolean isLightOn() {
        return wagons.get(numberOfWagon).isLight();
    }

    @Override
    public boolean isLength(int expectedLength) {
        return wagons.size() == expectedLength;
}
}*/